import React from 'react';
import {View, StyleSheet} from 'react-native';

const SkorScreen = () => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({})

export default SkorScreen;
